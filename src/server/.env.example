# Redis Configuration
# For local Redis: redis://localhost:6379
# For Redis Cloud: redis://default:password@redis-endpoint.com:port
REDIS_URL=redis://localhost:6379

# Anthropic API Key (REQUIRED for orchestrator)
ANTHROPIC_API_KEY=your-api-key-here

# OpenAI API Key (REQUIRED for smart experimentation agent)
OPENAI_API_KEY=your-openai-api-key

# Modal Configuration (REQUIRED for experiment execution)
MODAL_TOKEN_ID=your-modal-token-id
MODAL_TOKEN_SECRET=your-modal-token-secret

# Server Port (optional, defaults to 8000)
PORT=8000

# Server URL for orchestrator callbacks (if running on different host)
SERVER_URL=http://localhost:8000